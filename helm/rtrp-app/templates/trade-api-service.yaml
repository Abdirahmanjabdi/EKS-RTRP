apiVersion: v1
kind: Service
metadata:
  name: trade-api
spec:
  selector:
    app: trade-api
  ports:
    - port: 80
      targetPort: {{ .Values.tradeApi.port }}
      protocol: TCP
  type: ClusterIP